import 'package:flutter/material.dart';

class CpuInput extends StatelessWidget {
  const CpuInput({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('CpuInput');
  }
}
